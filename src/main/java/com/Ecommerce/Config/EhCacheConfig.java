
  package com.Ecommerce.Config;
  
  import
  org.springframework.context.annotation.Configuration;
  
 
  @Configuration //integrate with EhCache public 
  class EhCacheConfig {
	  
	  
	 
	  
  
	/*
	 * Logger log=LoggerFactory.logger(ProductService.class);
	 * 
	 * private net.sf.ehcache.CacheManager cacheManager;
	 * 
	 * @PreDestroy public void destroy() { cacheManager.shutdown(); }
	 * 
	 * 
	 * @Bean CacheManager cacheManager(){ return new
	 * EhCacheCacheManager(CacheManager().getObject()); }
	 * 
	 * private EhCacheManagerFactoryBean CacheManager() {
	 * log.info("Starting Ehcache"); EhCacheManagerFactoryBean factoryBean = new
	 * EhCacheManagerFactoryBean(); factoryBean.setConfigLocation(new
	 * ClassPathResource("ehcache.xml")); factoryBean.setShared(true); return
	 * factoryBean; }
	 */
  
  
  }
 